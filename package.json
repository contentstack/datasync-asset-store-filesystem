{
  "name": "@contentstack/datasync-asset-store-filesystem",
  "version": "1.0.0",
  "description": "Retrieve asset from sync manager ans store/remove it from filesystem.",
  "main": "./dist",
  "types": "./typings",
  "homepage": "https://github.com/contentstack/contentstack-asset-store-filesystem#readme",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "keywords": [
    "Contentstack",
    "utility",
    "connector"
  ],
  "scripts": {
    "clean": "rimraf dist coverage",
    "build-ts": "npm run clean && tsc",
    "watch-ts": "npm run clean && tsc -w",
    "test": "jest --coverage",
    "tslint": "npx tslint -c tslint.json 'src/**/*.ts' --fix",
    "compile": "tsc",
    "prepare": "npm run compile",
    "pretest": "npm run build-ts && rimraf _contents coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/contentstack/contentstack-asset-store-filesystem"
  },
  "bugs": {
    "url": "https://github.com/contentstack/contentstack-asset-store-filesystem/issues",
    "email": "support@contentstack.com"
  },
  "author": "Contentstack LLC <support@contentstack.com>",
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.17.11",
    "mkdirp": "^0.5.1",
    "request": "^2.83.0",
    "rimraf": "2.6.2",
    "debug": "^4.1.0"
  },
  "devDependencies": {
    "@types/jest": "^23.3.10",
    "@types/node": "^10.12.2",
    "jest": "^23.6.0",
    "rimraf": "^2.6.2",
    "supertest": "^3.3.0",
    "typescript": "^3.2.2"
  }
}
